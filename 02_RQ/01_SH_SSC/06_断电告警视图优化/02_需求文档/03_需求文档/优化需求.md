```
有实时告警为消除就会闪
	红色 - 可以跳转到实时
	只要不消除就会一致闪
	拉实时接口判断即可
	

保证弹框还是要保证
	


如果绿色
	也可以跳转了（跳到实时断电）
	全部消除后就满足
```





```
实时告警级别
	写死2级即可 - 历史也是
	
	后端写死即可
```









```
1. PDU断电告警UI优化​​

在告警视图中，为PDU断电告警增加固定链接。
告警状态下，告警图标持续闪烁（即使已读），待告警清除后图标变为绿色正常状态且保持不变。
点击告警链接可跳转至详情页面。
解决方案：前端通过实时接口轮询判断是否仍有未解决的PDU断电告警。
​​2. 告警级别补充​​

需在“实时断电告警”和“历史断电告警”列表中，为PDU断电告警事件增加一个固定的告警级别。
该级别统一设置为“二级”
```

