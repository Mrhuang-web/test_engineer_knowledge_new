# 01总结

```

```



# 02补充

```
数据从综资-综资视图汇总过来的
	因此需要考虑的点：
		1、如果区域编码对不上，是否会统计过来
		2、如果批次号对不上？
		3、如果站点类型对不上？
		4、如果未匹配？
		5、是否需要同步？
		6、同个索引里面，存在不同批次号
		7、直辖市统计与跳转情况
		8、存在1对多或多对1的情况（动环属性报表、动环空间表）
		9、存在A对B省份不同，B对A省份不同
	造数的点：
		1、是否需要具体到区县编码
		2、综资视图取数是怎么取得？





select batch_num from zz_data_sync_info where `table_name` = ? limit 1     ods_zz_site_property(String)
select area_code as precinctCode, precinct_name as precinctName from t_cfg_precinct where length(precinct_id) = ? and area_code is not null     Parameters: 5(Integer)

select area_code as precinctCode, precinct_name as precinctName from t_cfg_precinct where length(precinct_id) = ? and area_code is not null     8(Integer)

select area_code as precinctCode, precinct_name as precinctName from t_cfg_precinct where length(precinct_id) = ? and area_code is not null     11(Integer)

select batch_num from zz_data_sync_info where `table_name` = ? limit 1    ods_zz_site(String)

```

