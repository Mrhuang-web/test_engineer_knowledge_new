1、汇聚机房的数据来源
	综资-站点动环属性表中-动环专业站点级别为传输节点的
	通过站点动环属性表中-动环专业站点级别为传输节点的 与 站点空间表进行匹配
		疑问点：
			是通过站点再到空间（测试中，造一条站点有，但是空间内没有对应的 - 然后看是否会被统计进来）
			还是先空间表，再到站点（测试中，造一条空间有，但是站点内没有对应的 - 然后看是否会被统计进来）
			
			验证：
				是从站点属性 -> 到空间的（如果站点属性没有，那么空间也就没有）


2、目前现网的验证方法
	综资-原始数据（）
		1、找动环属性表，筛选出动环专业站点属性为传输节点的（复制到excel中）
		2、通过站点名，到空间表-站点报表中，一个个站点筛选，看日期是否一致（即统计进来的站点数是否一致）
			看下接口，看int_id啥的是否重复还是对不上等情况
		3、然后跟汇聚机房转供电里面进行比较
	
	
	
	目前跳转到综资视图
		综资视图不是按动环属性的，因此可能不一致
		现在综资视图里面有两个索引(左上角站点类型筛选-是综资站点属性表的，右侧查询条件是站点空间里面的)
		因此两个筛选存在冲突
			目前解决清空就是把综资全部拉到数据库？
			还是怎么解决
			

3、目前存在问题
	1、汇聚机房转供电存在汇聚站点总数 与 原始数据对不上（即动环属性=传输节点的）   -- 上海目前是有问题的、上海的汇聚统计也有问题、站点数量也有问题
	2、综资视图跳转（跳转直辖市存在问题，没有展示）
	3、如果空间没有，但是站点的-zh_label直接跟映射表匹配的话也会回显
		批次一定要一致
	
	

4、逻辑更正
	测试环境中验证过：
		1、如果缺失对应空间，站点有也不会统计；如果缺失站点，空间有也不会统计
		2、但现网上海统计出来的数据变多了
		3、有些城市统计又少了
			有些动环属性是匹配不上的（没有对应的空间）