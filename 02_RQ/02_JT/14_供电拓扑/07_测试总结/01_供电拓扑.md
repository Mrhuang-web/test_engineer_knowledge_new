# 01总结

```
资源设备：目前是1对1

```







# 02补充

```
1、拓扑图和动环数据关联（历史遗留）
	图标只显示资源的设备

	1、告警状态 -- 要跟动环关联到设备资源后 就会有标识，没有关联就不用
	2、资源和设备关联后 -- 鼠标悬停可以展示基础信息（资源、性能、告警）
		基础信息 -- 取资源信息（综资-原始视图-下钻到设备，即可看到资源信息）
					匹配不到，仅展示综资名称，
		性能数据 -- 跨拓扑
						（资源1个设备，可能对应动环多个设备）
						（如果有多个设备关联的时候，是需要可以切换的） 
							--- 注意资源名要展示正常
							--- 默认选第一个
							--- 如果是3项（未合并）就逐条查看
							--- 目前先展示几条
					确实重要
						（合并显示：只展示三项对应的数据，/合并展示即可）
						（没合并：逐条展示）

					调用实时数据
		告警信息 
				逐个展示（按原型图）


		数据关联
				power_device_id 进行关联


		1对多：
				动环设备多个 -- 对综资设备1个
		多对1：




资源设备与动环匹配
	匹配1对1
	匹配1对多
	站点维度下，power才关联



数据展示：
	现网汕头 -- 有数据
```

