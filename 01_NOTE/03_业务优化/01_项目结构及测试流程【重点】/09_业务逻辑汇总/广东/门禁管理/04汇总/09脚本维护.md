# 01力维时间

```
udp_protocol.py
	根据接收的指令进行过滤是否要启动事件
	# 检查并更新响应数据（事件处理）
    rule_response = event_center.check_and_update_response(parsed_data, rule_response)
    

event_center.py
	事件指令逻辑判断
	

根据配置来启动事件模式，以及事件类型配置
	events.json
	protocol_configs.json
	
	enabled 字段的作用是控制每个协议配置是否启用，而不是控制整个事件中心的开关。这样，您可以为每个协议配置单独设置是否启用，更加灵活。
	想启用 liwei_udp 协议的事件处理，只需要将 protocol_configs.json 文件中 liwei_udp 配置的 enabled 字段设置为 true 即可，不会影响其他协议的配置
```





```
主要排查文件：
	udp_protocol.py
	tcp_protocol.py
```

