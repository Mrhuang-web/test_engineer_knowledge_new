# 01总结

```
服务：


逻辑：
	每天凌晨-在综资同步完成之后，再进行配置同步（根据已经配置的配置表，同步当前综资的服役年限，并重新计算超期服役）
	
	前端新增了权限菜单  -- 需要给角色添加超期服役更新的权限才能修改
```



# 02补充

```
前端有配置，内部配置
	1.1 - 1.4


	把已有的进行初始化 - 同步到编辑页面里面（这样就能同步知道都是同一份）
	后续就知道改什么（就以页面的数据为主了）

	最上级 - 左上角填包后，会自动全部填上（每种类型都是 -- 只以类型维度）
		倒着填是否会填？（无论如何，只要填了左上角第一个，就会全部覆盖）

	做组件级别 -- 要在角色权限中可以配置



补充问题

	后端：
		1、目前修改配置，超期服役详情表数据的更新周期没做变动
		2、目前配置里面没做位数校验（且负数也要检验，为0，字母子类的）
		3、目前配置设备类型和子类存在缺失（待产品确认）
		4、目前配置年限初始化不与服役表一致（目前还未同步）
		5、修改后，设备子类也应该集成


		6、更新周期同步后，超期服役分段没有更新
		7、初始化后，有些站点没有时间，也统计上了（不同的设备子类，不同更新周期时，初始化后全变成一样的？表也变了）   -- 锂电池（开关电源锂电池）

		问题1：锂电池（开关电源锂电池）- 超期服役详情中数据中心并没有值，但是初始化给了年限需排查
		问题2：更新周期更新后，服役年限分区段没有重新计算

		不清除再次触发，是否新增
		修改后，再触发
		不修改重复触发后，表变更了？
	前端：
		1、位数校验（且负数也要检验，为0，字母子类的）
		2、数据中心填写后，其他未同步


	额外漏洞：
		1、目前超期服役分段的计算，没有精确到3位，导致匹配还是有些不准确
		2、目前超期服役没有改同步的表，导致现在今天修改的配置，明天照样要配置新的（因为一直都是同步的旧的那张表）
			eg：
				用户今天配置了某个设备的周期为10（详情表今天更新周期会更改，且超期服役分段会改） -- 新的字典配置表也改了时间
				第二天，综资又与旧配置表匹配，返回的那就又是旧的了（这个时候可以用目前的表去同步详情表即可）
```

