# 01提测

```
云南 - 容量
	使用集团的es，日期要传入当天前一天或更前的时间，（数据时间也要一致）


	ups容量计算的问题，验证打包（下面是两个包，两个分支）：
		部署：capacity-service服务
		gemc（develop_capacity_1121）     如果失败，先打GEMC_ALL 好像没办法直接在项目里面收到，要一个一个看（然后在这里面打develop_capacity_1121），然后再打gemc的
		ynems（develop）


	问题：
		问题1：4楼2#UPS系统，通过公式1电流的计算方式，取到的最大值应该是150，而不是图中的149.
		问题2：4楼2#UPS系统，这个计算详情里面对应设备的输出电流详情数据，并不是包括最大值。因此对应的数据更新时间也不是最大值上送的时间。




	ups
		1、配置：基础信息维护：批量维护
		2、容量管理：UPS容量分析：当前负载率汇总报表：容量详情

		一个系统下需要有两个ups

			00531006000005759877
			01-32-01-02-01-03，四楼电力室1#艾默生(EMERSON UPS)(NXR)

			SELECT * FROM t_cfg_device WHERE device_name = '四楼电力室1#艾默生(EMERSON UPS)(NXR)';
			SELECT * FROM t_cfg_precinct WHERE precinct_name = 'KJ-DL-ZHT0003_昭阳区全球通大楼四楼动力机房(test)';
			SELECT * FROM t_cfg_precinct WHERE precinct_id = '01-32-01-02-01';
			SELECT * FROM t_cfg_metemodel_detail WHERE model_id = '00001008000000010762';





		curl "http://10.12.7.157:3105/v1/capacity/testCapacityStatistic?date=2025-11-30&precinctId=01-32-01-02-01&sysIds=da8ed141-a97b-4b28-aa2b-0127ea952e5e"			# 这个用不了，只能传入01
		
		curl 'http://10.12.7.157:3105/v1/capacity/statisticCapacityByDateAndPrecinctId?precinctId=01-32-01-02-01&date=2025-12-30&namespace=alauda'						# 现在用这个，可以支持站点级别

```

