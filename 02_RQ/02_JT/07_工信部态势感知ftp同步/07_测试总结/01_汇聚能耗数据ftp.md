# 01总结

```
需求概述
	1、就是新增英语字段
	2、能够正确统计指定月，会自动统计为，改月份前三个月的数据
	3、文件时间，以及字段内容与需求中一致



涉及库表
	注意：目前能耗报表这块，都是手工维护的，涉及一下内容
	
	select * from energy_access_site_config;【以站点维度】
	select * from energy_access_site_build_config;【以核心机楼＋楼栋维度 -- 现在常用的】
	select * from t_scheduled_task;【这里主要记录定时任务，可以修改定时任务触发时间、只需要改这里面的就会自动生成】
	





测试方法
	本次用的是第一种，对比月度统计表的数据
	1、主要根据与能耗月报中，月度统计表进行比较即可【选择对应月份范围，然后统计对应数据】
	2、也可以看看板地图里面的投产初期【或是看能耗月报里面的也行，这个无所谓都差不多】





测试数据准备
	【这一块好像是es写入，还有如果是自己接入站点那么需要C接口好像-这个不太清除】
	1、目前是用了自有的数据、黄河数据中心这些
	2、报表中联系人【是在运维-通知通报里面设置的】
	3、报表触发
		

```

