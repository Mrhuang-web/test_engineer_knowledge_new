# 01总结

```

```





# 02补充

```
提测内容
	综资站点/机房映射索引（ods_zz_irms_site_map，ods_zz_irms_rom_map）结构变化
	1.供电拓扑 
	2.综资站点/机房入库
	3.超期服役

//触发scada抽取任务
	curl --location --request GET 'http://10.1.202.8:30655/v1/topuConfig/mappingResourceAll'


// 触发综资站点入库
	curl --location --request GET 'http://10.1.202.8:31640/migration/v1/zzSyncData/saveZZData?orderIds=SSSP-20241031-000010' \
--header 'User-Agent: Apifox/1.0.0 (https://apifox.com)'


// 触发综资机房入库（有问题）
	curl --location --request GET 'http://10.1.202.8:31640/migration/v1/zzSyncData/saveZZData?orderIds=SSSP-20241031-000018' \
--header 'User-Agent: Apifox/1.0.0 (https://apifox.com)'


	设备（没问题）
	curl --location --request GET 'http://10.1.202.8:31640/migration/v1/zzSyncData/saveZZData?orderIds=SSSP-20241031-000025'


// 触发超期服役
	curl --location --request GET 'http://10.1.202.8:32119/v1/hiddenDanger/startOverdueAll' \
--header 'User-Agent: Apifox/1.0.0 (https://apifox.com)'
	
涉及服务
    scada,external, hiddendger
涉及脚本

涉及配置






涉及功能：
	触发综资站点入库、触发综资机房入库
		检测分析统计查询中 - 站点、机房是否对的上，设备是否也对的上 -- 在原始数据中（空间、属性表是否有对应的 - 设备呢）

		涉及设备入库：
			汇总的时动环的数据 - >  只是说加入了综资的匹配（但还是相当于左表是动环的设备表，知识匹配上的就会显示综资的设备而已）

		机房入库问题：
			有部分站点可能机房属性存在，机房中不存在

	超期服役：
		通过id匹配入库的（只要用动环机房的precinct - 匹配到引射表dh_id -> 就可以直接拿对应综资 设备拿下来回显）
		overdue_device_type_dict
		精确到站点或机房 - 根据站点统计综资对应站点下设备的数量是否一致
		汇总数据（存在 - 超期服役包含了3和4的类型 --因此直接统计前三个就行了，不然导致重复）

	供电拓扑：
		（站点匹配就行？）
		通过id匹配入库的（只要用动环机房的precinct - 匹配到引射表dh_id -> 就可以直接拿对应综资 设备拿下来回显）  （没有对应precinct_id就会被过滤掉 - 机房的precinct_id）
		站点precinct - 综资dh_id - 机房precinct_id -> 综资dh_id -> 动环链路（room、site id） -> 设备表对应设备

		在原始数据：
			专业链路也可以查到
			然后再看对应设备是否存在的
```

