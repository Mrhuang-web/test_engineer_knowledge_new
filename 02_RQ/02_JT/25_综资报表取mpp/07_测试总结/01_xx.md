# 01_总结

```

```





# 02_补充

```
日志具体查询日志，看mpp服务
```



# 03_问题

```
机房、属性表、设备都有问题（单独去查的）

存在匹配问题
	eg
		机房表：
			根据机房已经有站点关联的才会匹配进来
			但是站点存在多条件记录，机房也会变多条记录
			
			
综资-原始数据
	设备类型
		存在1个设备 --> 对应多个机房站点
		（省份编码不一致的情况，正常是以设备省市区编码为主 - 匹配下来只有一条）


属性表（就是记录综资的那些信息？ - 先匹配动环？ - 再综资站点？）
	有可能存在上级不存在的情况（这个需要考虑进去，上级和上上级不存在）
	省市区编码不一致时
	power_monitoring_site_name不一致时，不匹配？
	存在名称一样，但是省市区不同怎么处理
	
	需要跟动环的匹配，最终再入库都综资表（属性表 - 其实就是动环和综资都存在的设备）
```

