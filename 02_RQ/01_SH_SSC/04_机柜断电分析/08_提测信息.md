# 01提测

```
需求概述

	v2-sc-m-sh-service


涉及库表





测试方法
		curl --location 'http://10.12.12.161:31241/v1/energyCabinet/powerOutage/history/handlePowerRecovery' \ --header 'Content-Type: application/json' \--data '{}'



		curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"key1":"value1","key2":"value2"}' \
  http://127.0.0.1:9102/v1/energyCabinet/powerOutage/history/handlePowerRecovery



		语音：
			首次以登录（打开页面为对比时间）
			后面以前一次调接口时间

		语音展示：
			需要配置角色权限（且时间要晚于登录时间，或是上一次接口调用时间）

			不生效后，触发还会有声音





逻辑变更
	现在数据写入到cabinet_history，判断值，成功就alarm_statisfied就为1 -- 后续就不判断综合，直接判断单路
	触发后写入energy_cabinet_poweroutage_current这家拍卖行
```

